<build>
    <plugins>
        <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-compiler-plugin</artifactId>
            <version>3.8.1</version>
            <configuration>
                <source>1.8</source>  <!-- Update this to a supported version like 1.8, 11, or 17 -->
                <target>1.8</target>  <!-- Update this to a supported version like 1.8, 11, or 17 -->
            </configuration>
        </plugin>
    </plugins>
</build>
